{
  "name": "cookie-pulse",
  "version": "1.0.0",
  "description": "Live on-chain terminal for Cookie Chain (SVM) — chain telemetry, pool census, wallet inspector and real transactions.",
  "main": "server.js",
  "scripts": {
    "start": "bash start.sh",
    "check": "env -i PATH=/usr/bin:/bin HOME=$HOME node --check server.js"
  },
  "keywords": ["cookie-chain", "solana", "svm", "web3", "defi", "analytics", "capp"],
  "license": "MIT",
  "dependencies": {
    "@meteora-ag/dynamic-bonding-curve-sdk": "^1.2.6",
    "@solana/spl-token": "^0.4.9",
    "@solana/web3.js": "^1.95.4",
    "bn.js": "^5.2.1",
    "bs58": "^6.0.0",
    "express": "^5.1.0"
  }
}
